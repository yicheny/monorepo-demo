{
  "name": "monorepo-demo",
  "version": "0.0.1",
  "description": "a monorepo project demo",
  "main": "index.js",
  "type": "module",
  "author": "ylf",
  "license": "ISC",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 0",
    "start demo": "cd ./packages/demo && npm run start",
    "start shared": "cd ./packages/shared && npm run start",
    "build shared": "cd ./packages/shared && npm run build",
    "start yc-ui": "cd ./packages/yc-ui && npm run start",
    "build yc-ui": "cd ./packages/yc-ui && npm run build",
    "publish release-demo": "cd ./packages/release-demo && npm run upload",
    "publish:patch release-demo": "cd ./packages/release-demo && npm run publish:patch"
  },
  "devDependencies": {
    "execa": "^6.1.0",
    "minimist": "^1.2.7"
  }
}
